#ifndef PARSER_HPP
#define PARSER_HPP

#include <string>

std::string insert_concat_symbol(const std::string);
std::string to_postfix(const std::string);
std::string to_infix(const std::string);

#endif /* ifndef PARSER_HPP */
